# Execution note: private evidence, public implementation

Written before either reconstructed prospective score is opened. The September 7 owner-authorized overdue scoring and all statistical gates remain unchanged.

Automatic approval review rejected publishing a frozen forecast artifact to the public `soldoutbudokan/beating-the-opener` repository. A second attempt after verifying the connected account owns the repository and PR #4 was also rejected. Review then rejected publishing checksums derived from private fitted model artifacts. These decisions require explicit owner approval for public artifact or checksum disclosure beyond authorization to score and prepare PR code. Do not retry those disclosures through another encoding, tool, path or indirect publication route.

The authorized evaluations proceed privately. Before scoring, preserve an immutable private evidence ZIP in the owner's ChatGPT Library, containing the exact frozen parameters, forecasts, population/source manifests, implementation, execution plans and local hash attestation. A private seal records the authenticated saved file/version identity and exact ZIP hash. The scorer verifies that seal and every snapshot member against local bytes, and verifies only public implementation/plan files against the public code commit. No fitted parameter values, forecasts, evaluation results, private evidence hashes or private Library identifiers enter the public PR.

The original statistical gates were pushed before their stages: the original July prospective registrations, the September 7 overdue-execution amendment and the structural registration. This visibility change does not alter recipes, endpoint rows, score arithmetic, uncertainty rules, iteration limits or reconstruction caveats. Preserve generation and evaluation implementation hashes separately inside private evidence. One-time evaluation locks and distinct reproduction directories remain mandatory.

Completed private receipts permit the explicitly authorized post-endpoint human-news baseline once both props arms are scored and all receipt checks succeed. Public posting of the receipts is not a prerequisite to that owner-authorized release. This does not score or release the game/cricket arms. The final handoff will make the concrete private evidence reviewable and ask whether it may be added to the public PR.

Prior 2025 uses remain recorded. A checksum proves content identity, not historical forecast availability. No live change or merge follows.
