# Owner-authorized recovery of unavailable WNBA v2 artifacts

The owner has asked us to recreate artifacts lost from the working environment.
The public implementation and exact historical input manifest remain available;
the former private fitted bundle and its saved executions are unavailable to
this session. Do not fabricate a replacement for that original storage receipt,
rewrite the earlier stopped experiments, or backdate a new candidate freeze.

This is a separately bounded reconstruction, not another model search. Publish
this amendment and its reviewed implementation before empirical execution.

1. Restore the exact public manifest at repository commit
   `64acad8248ef6536ef7c655d733293f6763263cc`, path
   `wnba/research/rich_context/results/source_manifest.json`. Its SHA-256 is
   `1fedab483827cca1d4887dce270a63e124fe4d4edf3c1786adcff7104025a132`.
   Verify every selected raw asset against its pinned URL, length and hash.
   Record actual fresh download clocks separately from the historical manifest.
2. Reconstruct exactly one inherited recipe with the unchanged original engine:
   `fit(observations, through_season=2022, shrinkage=1, variant="box")`, using
   the original source adapter and eight-hour historical availability assumption.
   Verify the pinned original engine files before fitting. No through-2024 final
   fit, second shrinkage recipe, historical market-price replay, 2025 evaluation
   or protected 2026 data is part of this recovery.
3. Run the unchanged participation repair, registered legacy-source quarantine,
   calibration on 2015–2022, checks on 2023 and fixed sensitivity on 2024.
   Preserve the scalar estimators, populations, count distributions, source/state
   checks, decision gates and fallback defined in the existing registrations.
4. Allow one primary execution and one independent saved-input reproduction,
   each in a new exclusive directory with at most 600 seconds, including the
   inherited-coefficient reconstruction and comparison. Downloading input bytes,
   dependency installation and synthetic software checks precede those clocks.
   Record every start, completion or interruption. No automatic empirical retry,
   new candidate, rounding to force equality or threshold adjustment is allowed.
   Original experiment budgets remain closed and their prior receipts unchanged.
5. The original private candidate identity and selected mode, already recorded
   in the conversation, are supplied in an immutable private expectation file
   before execution. The reconstructed candidate must match them exactly and
   both new executions must have byte-identical substantive artifacts. A mismatch
   produces a discrepancy receipt and blocks a candidate freeze. Matching a
   recorded recipe hash does not recover or authenticate the missing old ZIP.
6. A separate reconstruction receipt records exact inputs, code, dependencies,
   numerical runtime, coefficients, clocks and reproduction. It explicitly says
   that the original archive identity has not been verified. Existing original
   bundle/seal checks stay intact on the original execution path.
7. Only after matching reconstruction and reproduction may the four pinned 2025
   assets be opened for a frozen inference seed. They cannot change candidate
   selection or provide new evaluation results. Create a new candidate ID and
   current freeze clock, retaining the old unavailable freeze as history.
8. Persist the rebuilt bundle and complete reconstruction inputs/evidence before
   activation. A successful write does not establish verified retrieval. Verify
   exact restoration from an authorized durable destination before admitting
   future forecasts. No prior durability clock or forecast admission is implied.

This amendment authorizes recovery code and fixed historical reconstruction.
It does not authorize disclosure of private fitted parameters or evidence,
change any live routine, place wagers, qualify a model, or establish an edge.
The owner's instruction to complete all three stages before a PR remains in
force; source-code commits to the existing work branch are preparatory work.
