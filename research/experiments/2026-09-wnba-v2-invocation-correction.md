# Invocation correction before source values or fitted results

The first participation-v2 invocation failed at the initial parquet reader
because its Python import path omitted the already installed pyarrow dependency.
The ImportError occurred before a parquet table, repair cohort, calibration
scalar or check-season result was loaded or computed. The implementation,
registration, inherited recipe and data selection are unchanged.

Preserve the interrupted directory, inherited-recipe copy, exception traceback
and runtime receipt. Charge one full second conservatively against the primary
600-second budget. Correct only the dependency import path, verify the installed
parquet engine without reading empirical source values, then permit one corrected
invocation in a new exclusive directory with a maximum of 599 seconds. The one
exact saved-input reproduction retains its original 600-second limit.

This is an explicit invocation amendment before the corrected run, not a new
model attempt or a reset of the empirical budget. All source, comparison,
selection, protected-data and disclosure rules remain unchanged. A further
failure must remain visible and cannot trigger an undocumented retry.
